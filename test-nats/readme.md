# NatsIO

## cmd

``` sh
cd /home/chris/git/learn-golang/test-nats

sudo docker-compose -f docker-compose-cluster.yaml up -d
sudo docker-compose -f docker-compose-cluster.yaml down

sudo docker-compose -f docker-compose-cluster-jetstream.yaml up -d
sudo docker-compose -f docker-compose-cluster-jetstream.yaml down
```
